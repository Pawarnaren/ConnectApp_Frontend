import React from 'react';
import '../style/Loader.css'; // Include your own CSS file or adjust styles as needed

const Loader = () => {
  return (
    <div className="loader-container">
      <div className="loader"></div>
    </div>
  );
};

export default Loader;
